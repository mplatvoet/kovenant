general:
  branches:
    ignore:
      - gh-pages
#  artifacts:
#    - "build/libs"

test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/build/test-results/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
    - ./gradlew libs

#TODO make this publish the docs
#deployment:
#  circleci:
#    branch: master
#    commands:
#     - ./publish.sh

