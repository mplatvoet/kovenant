general:
  branches:
    ignore:
      - gh-pages
  artifacts:
    - "build/libs"

deployment:
  libs:
    branch: master
    commands:
      - ./gradlew libs
  docs:
      branch: master
      commands:
        - ./publish.sh